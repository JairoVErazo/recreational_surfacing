/** @type {import('next-sitemap').IConfig} */
module.exports = {
    siteUrl: 'https://recreational-surfacing.com',
    generateRobotsTxt: true, // Genera también un archivo robots.txt
    sitemapSize: 5000, // Número máximo de URLs por sitemap
  };
  